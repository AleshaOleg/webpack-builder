@import '../../styles/colors.pcss';

.index {
  &__header {
    font-size: 16px;
    font-family: 'Roboto', sans-serif;
  }
  &__footer {
    font-size: 16px;
    font-family: 'Roboto', sans-serif;
  }
}
